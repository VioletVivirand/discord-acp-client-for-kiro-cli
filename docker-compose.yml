# Compose wrapper for the bot image. Usage:
#   cp .env.example .env            # set DISCORD_TOKEN
#   docker compose build
#   docker compose up -d
#   docker compose exec bot kiro-cli login   # one-time Kiro auth
services:
  bot:
    build: .
    image: discord-acp-kiro-bot:latest
    container_name: discord-acp-kiro-bot
    env_file: .env
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    volumes:
      - discord-acp-kiro-data:/home/bot/.kiro

volumes:
  discord-acp-kiro-data:
